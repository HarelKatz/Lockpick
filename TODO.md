# Lockpick — Active Tasks

> **Edit rules:** A task pool, not a sequential roadmap. Pick freely within a tier; `## Now` outranks `## Next` outranks `## Later`. An `After:` line marks an *open* hard prerequisite — don't start until it ships; no `After:` = independent. When a bug fix introduces a new invariant, move that invariant to ARCHITECTURE.md. Keep specs short; detail belongs in commits.

**Status:** Full stack shipped — 49 file types / 44 parser classes, host merge (manual + auto), one-click collection workflow. See git history for completed work.

## Now

> **Testing-foundation initiative (do first).** Origin: the time-slider shipped green but had 4 hand-caught bugs (drag-only jitter, floating nodes, model-completeness gap, isolated-host case) because e2e specs assert the `window.__lockpick_graph__` data model only (blind to CSS/layout + real drags). Goal: a quality *loop* whose spine is **invariants over a normal op** — general properties true for ANY op — so using the app produces a failing test. Full design in git history / the approved plan. Order: op-builder first (keystone), then the other three (independent).

- **Shared REST op-builder** — Create `tests/opbuilder/` (`rest.py`, `shapes.py`, `profiles.py`); test-only, imports no production/pytest code so `tests/e2e/seed_e2e.py` can use it under `uv run python`. `OpBuilder(client)` mirrors the REST surface and works with BOTH pytest `TestClient` and `httpx.Client` (assert per-endpoint status codes, not blanket 2xx); `apply_topology()` = the hosts→uploads→connections loop; `profiles.normal()` reuses the static `tests/fixtures/network/topology.json` + the manual/undated/isolated additions currently in `seed_e2e.py` (so `normal()` *is* the e2e seed). `profiles`: empty/minimal/normal/edge_cases/scale(n). Migrate the `loaded_op` fixtures in `tests/test_scenario_network.py` + `tests/test_scenario_random_network.py` and `seed_e2e.py:main()` onto it. Done when the ~20 scenario assertions + `make test-e2e` pass with zero assertion edits. Keystone — the invariant work (BACKLOG) builds on it.
- **Frontend unit layer (vitest)** — No frontend unit runner exists today (Playwright-only). Add `vitest` devDep + `frontend/vitest.config.ts` (`environment: 'node'`, `globals: true`) + `test:unit` script. Mechanically extract pure logic from `frontend/src/pages/GraphView.tsx` + `frontend/src/components/GraphCanvas.tsx` into `frontend/src/utils/`, keeping the `useMemo(() => fn(...), [deps])` wrappers: `timeWindow.ts` (computeEdgeTimes / computeTimeDomain [min===max→null] / computeTimeStep / deriveWindow / computeHiddenEdgeKeys [key-match + undated exemptions] / computeHiddenNodeIds [disconnected-but-not-isolated] / snapTimeHandle / reclampWindow [disjoint→full]); `graphMerge.ts` (mergeGraphResponses); `edgeDisplay.ts` (computeEdgeLabel, confidenceColor); `layout.ts` (force/dagre/grid/circle/initialLayout). Exhaustively unit-test the time-slider math. The existing e2e suite is the regression gate for the extraction; do NOT delete e2e coverage (it catches the layout half).
- **Test doctrine in CLAUDE.md** — Add two sections (no new .md per project rule). "Feature Design Pre-flight Checklist": 4 blind-spot classes as pre-build questions — (1) interaction modes (click *and* drag *and* keyboard; onChange ≠ real drag), (2) layout/rendering (resize a neighbor? scrollbar? overflow? jitter? — the hook can't see this; assert `boundingBox`/`scrollHeight`), (3) model completeness (empty/one-host/undated/isolated/self-loop/null-host/long-nickname; every "always/never" claim is an invariant), (4) data profiles (holds at 5 *and* 200?) — plus a living **Misses log** ("when a bug ships green, add a line naming its class + the invariant that now covers it"). "Test-Layering Doctrine": the loop (design→build+test→verify→use/explore→feed back); layers cheapest-first (unit → property → scenario → e2e-invariants → agentic-explore); `profiles.normal()` is the default substrate; accepted walls (canvas pixels, aesthetics, headless≠real-browser).
- **Dial the random generator to 5–200** — Parameterize `tests/generate_random_network.py` `build_random_topology(rng, *, n_hosts=None, n_keys=None)` (replace hardcoded `rng.randint(5,8)`; `n_keys=max(2, n_hosts//8)`, pivots ∝ hosts); add `--hosts N`; add an in-memory topology export (skip real RSA keygen for structure-only invariant runs — fake fingerprints); add `test_generator_determinism` golden-hashing the *structural* topology (not RSA keys). Preserve single `Random(seed)` draw order (new draws after existing). Feeds `profiles.scale(n)`.

- **Fix `createHostNote` double-submit race** — the "Add note" action in `HostDetailSidebar` (onClick + Ctrl/Cmd+Enter) is guarded only by React state (`setNoteAdding`), not a `useRef` flag, so rapid double-activation in the same tick dispatches concurrent POSTs → duplicate notes. Apply the `useRef` double-submit guard per CLAUDE.md → Frontend Conventions.
- **Engagement report export** — `GET /ops/{op_id}/report?format=markdown|html`: structured engagement summary (op metadata, host inventory, credential inventory, pivot paths via `find_paths`, sudo escalation summary, activity timeline). Markdown primary; HTML wraps it in a minimal printable template. Full credential values are shown — Lockpick never redacts, including in reports (ARCHITECTURE.md Rule #4).

## Later

- **Credential blast-radius rollup** — per-credential "unlocks N hosts across Y users", surfaced in the credential detail panel and list row, derived from existing `CredentialLink` data (no schema change).
- **Op summary + briefing fields** — Operation gains two markdown fields: `summary` (short, rendered in op header) and `briefing` (long, collapsible in op detail view). Both user-editable, optional. Requires two new columns + an Alembic migration.
