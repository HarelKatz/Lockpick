# Lockpick — Backlog

> **Edit rules:** Future / large / conditional work — not scheduled. Promote an item to TODO.md when you commit to it. Keep entries short; rationale may run longer for items whose "why" isn't obvious.

- **MCP server** — standalone `mcp/` package (does **not** import from `backend/`) exposing op data over MCP/stdio so an AI agent can help navigate pivots. Talks to the Lockpick REST API over HTTP; configurable via `LOCKPICK_URL`. Implement only when all prior work is stable.
- **Frontend graph/layout invariant suite** — Testing-foundation initiative; **prerequisites shipped** (op-builder + frontend unit layer). **Partly landed:** the render-vs-hook invariant (`frontend/e2e/render-invariant.spec.ts`, drawn == visible across time-slider states, host toggles, and the BFS path filter), adding the dev-only `drawnNodeIds()`/`drawnEdgeKeys()` accessors (Rule #26). **Remaining:** `frontend/e2e/invariants.spec.ts` over `normal` + a generated `scale(50)` op. Assert the layer the `window.__lockpick_graph__` hook can't see, via hook + `boundingBox()` + net-new console capture: no floating node (every `visibleNodeId` `screenPos` in canvas bounds), no viewport overflow (no horizontal scrollbar), zero console/pageerror during a scripted sequence (drag slider, toggle hosts, open panels, shift-click), toggling a control never resizes a neighbor, every visible-edge endpoint ∈ `nodeIds`. Add to `frontend/e2e/helpers.ts`: `captureConsole(page)`, `allNodesInView(page)`, `assertNoNeighborResize(page, controlId, siblingId)` (generalize the time-slider "Reset never resizes track" test). New render-state extends the hook (Rule #26; never rename) + `graphState()` in lockstep.
- **Tiered test infra** — Testing-foundation initiative; `After:` the invariant suites exist. **Partly landed:** `make test-unit` (vitest), `make test-full` (all layers, serial default + `-j`-parallelizable), and the `make test` → `test-backend` rename. **Remaining:** pytest markers `slow`/`property` (register in `backend/pyproject.toml`); Makefile `test-fast` (`-m "not slow and not property" -n auto`), `test-invariants` (`-m property`), `test-scale` (`-m slow`), a sub-90s `gate` (test-fast + test-unit + fast e2e project); split Playwright into `chromium` (fast committed specs) + `chromium-invariants` (`scale(50)`; 200 nightly-only); keep `workers:1` (canvas not parallel-safe).
- **Agentic graph explorer skill** — Testing-foundation initiative, last phase; `After:` frontend invariant suite is stable. New `.claude/skills/graph-explore/SKILL.md`: a Playwright-MCP agent that seeds `normal`/`scale(N)`, drives the real browser (navigate/drag/evaluate-hook/console/screenshot) to hunt the aesthetic/rendering wall (jitter, overlap, off-canvas drift) — the "looks/feels right" gap deterministic specs miss — and distills every finding into a failing deterministic `*.spec.ts`/`test_invariants` case + a CLAUDE.md Misses-log row. On-demand only, never wired into `make gate` — a bug-FINDER feeding the deterministic layers, not the gate.
- **`systemd_journal` parser** — on journald-only hosts (no rsyslog / no text `auth.log`), the sshd Accepted-login, sudo, key-fingerprint and source-IP evidence Lockpick already mines from `auth.log` lives **only** in the binary journal (`/var/log/journal/<machine-id>/*.journal`), so Lockpick is currently blind to the auth/connection graph on those boxes. A journal parser would extend the existing pivot extraction to modern distros — same `ConnectionData` record shape as `AuthLogParser`, just a new input format. Collection: `.journal` is `0640 root:systemd-journal` (same privilege tier as `auth.log`'s `root:adm`, and usually `adm`-readable via tmpfiles ACL), so the sudo-free collector can typically `cp` the raw files. Build cost is real — binary format with object compression (LZ4/XZ) + optional FSS sealing; reference plaso's `systemd_journal.py` for the on-disk layout only (Apache-2.0; format reference, never an import — see the closed plaso/dissect parser-source evaluation).
